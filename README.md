Validates a deterministic FSA and checks whether it accepts or rejects a given string.

How to use:
Build and run "fsa.cpp" and then go to the file directory in the command propmpt and run "fas.exe input_string".
